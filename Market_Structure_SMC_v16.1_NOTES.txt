COMMENTS AND CHANGE NOTES STRIPPED FROM Market_Structure_SMC_v16.1.txt
The script itself carries no comments any more; everything that was in it is here,
in the order it appeared. Comments never counted toward Pine's compiled-token limit,
so this split is for readability and paste size, not for the token budget.
==============================================================================

// ═════════════════════════════════════════════════════════════════════════════
//  SMC MARKET STRUCTURE — 3-CANDLE PULLBACK ENGINE  ·  v15.23
//  + SESSION / DAILY / ANCHORED VOLUME PROFILES + KILLZONES + MTF TABLE
// ═════════════════════════════════════════════════════════════════════════════
//  v15.0 is v14.11 with the comments stripped, the retracement level display
//  removed, and fourteen defaults changed. NO rule, engine or alert behaviour is
//  different: the executable content is identical to v14.11 line for line apart
//  from those fourteen input lines and the deleted display block.
//
//  v15.23  THE COMPILED-TOKEN LIMIT, THE RUNTIME ERROR, A LIVE 50% LINE AND
//  TWO MASTER TOGGLES.  Built on v15.20; v15.21 and v15.22 are withdrawn.
//
//  1. "Compiled code contains too many tokens: 102987. The limit is 100256."
//     WHERE IT COMES FROM. f_tfPack is the whole structure engine written out
//     again for one requested timeframe, and Pine INLINES a user function at
//     every call site. The table calls it eighteen times, once per
//     request.security, so its 1,977 source tokens are compiled EIGHTEEN
//     times — over half the finished script. Anything inside it costs
//     eighteen times what it reads.
//     WHAT WAS GIVEN UP, and it was the cheapest thing available: the sweep
//     and fair-value-gap code behind the table\'s EVENT column, 230 tokens
//     inside f_tfPack and therefore about 6,100 compiled. That column was OFF
//     BY DEFAULT. Nothing else in the indicator used it — the sweep marks and
//     the structure everything else draws are untouched.
//     The "Show EVENT column" box is deliberately LEFT IN PLACE, relabelled
//     and inert, because deleting an input renumbers every input after it and
//     scrambles saved settings. Nothing is renumbered here.
//     IF MORE ROOM IS EVER NEEDED the lever is the same one: each timeframe
//     dropped from the table removes another whole copy of f_tfPack, worth
//     roughly 2,900 compiled tokens, and one fewer dataset to fetch.
//
//  2. THE RUNTIME ERROR IS FIXED. "The requested historical offset (382) is
//     beyond the historical buffer\'s limit (381), at f_hLine()", on XAUUSD 1m
//     at bar 14297. f_hLine draws the live iBOS*/nBOS* and 50% lines inside
//     "if barstate.islast", so it runs on one bar out of fourteen thousand.
//     Pine sizes each series\' history buffer by reading the code, cannot infer
//     a depth for a block that runs once, guesses small, and throws the moment
//     something reaches one bar further — which is why it was intermittent.
//     max_bars_back = 500 on the declaration is the documented remedy.
//
//  3. THE 50% LINE CAN BE TAKEN FROM THE LIVE LEG. "50% line measured from":
//       RANGE — halfway between the two live extended lines, as before. It
//         cannot exist until the broken side refills, which needs the 3-candle
//         rule, and by then price is often already back through the midpoint.
//       LIVE LEG — halfway between the running extreme of the leg and the
//         opposite level, FROM THE CANDLE THAT BREAKS. Both ends exist on that
//         bar: mRunHi resets to the break bar\'s high, and f_retExtInc
//         re-anchors the opposite line in the same bar.
//     It RATCHETS: a higher high moves the level up, a bearish candle does not
//     move it back down, because the extreme has not changed.
//     "Live leg anchor extends on" picks what counts as a new extreme — any
//     higher wick (the default, and what the retracement alerts use), or only
//     a candle that CLOSES past the previous candle\'s extreme.
//     One pair of settings drives all three tiers, and the percentage keeps the
//     meaning it has in Range mode: measured up from the lower of the two
//     prices, so 50 is the midpoint whichever way the trend runs.
//
//  4. TWO MASTER TOGGLES in group 0. Each is an override: ON forces its
//     behaviour, OFF hands control straight back to the individual settings,
//     which are never overwritten.
//       Structure — keep only the latest 10 marks per tier.
//       Structure — keep previous iBOS/iCHOCH and nBOS/nCHOCH.
//     THE HISTORY TOGGLE NEEDED ONE STRUCTURAL CHANGE. inHist is read deep in
//     the engine, long before any appended input exists, so the clear is now
//     DEFERRED: the engine raises _intClrReq and the deletion happens at the
//     end of the same bar, where the toggle is known. The result is identical —
//     a main event bar never prints an internal mark, so nothing drawn on that
//     bar is lost by clearing later. The inner clear was already flag-driven
//     (rqClear) and moved with it. NO code block is relocated, which is what
//     broke v15.21.
//
//  FOUR INPUTS ADDED, all appended at the end; not one existing input moves.
//  NOT ONE ENGINE RULE CHANGES.
//
//  v15.20  THE MTF TABLE STOPS AT THE HIGHEST TIMEFRAME THE CHART CAN
//  ACTUALLY FEED IT.  (v15.18 is withdrawn; this is built on v15.17.)
//    THE PROBLEM, measured rather than assumed. A probe requesting
//    bar_index + 1 from each timeframe reported how many bars request.security
//    really returns. On EURUSD, from a 1m chart, the weekly series came back
//    with NINE bars and the monthly with ONE. The per-timeframe engine is a
//    state machine that replays every pivot and break from the first bar it can
//    see, so one monthly candle does not make it fail — it makes it answer
//    confidently and wrongly. That is where the monthly row reading BULLISH on
//    a 15m chart and BEARISH on a 1h chart came from.
//    THE RULE, which fixes it by not asking the question. A row is drawn only
//    when its timeframe is at or above the chart AND at or below the ceiling
//    the chart can support:
//        chart 5m and below ....... ceiling 4h   (nothing daily or higher)
//        chart 15m ................ ceiling 1W   (daily and weekly, no monthly)
//        chart 10m, 30m, 45m, 1h .. ceiling 1D   (daily is the top row)
//        chart 2h and above ....... ceiling 1M   (everything, through monthly)
//    A 2m chart falls in with 5m and below, and the 61-to-119-minute band with
//    1h and below; neither is named by the rule and both take the conservative
//    side. THE 5m-AND-BELOW CEILING IS WHY: from a 1m chart the daily series
//    comes back with about SIXTY-NINE bars, and on the reported EURUSD chart
//    that row read BULLISH while the daily chart itself read BEARISH. Sixty-
//    nine bars is not a daily structure, so the row is no longer offered.
//    IT IS ALSO THE SECURITY-CALL OPTIMISATION. A row outside the ceiling has
//    its _uX flag false, and a false flag already collapsed that request to
//    timeframe.period — the chart\'s own data, already loaded, costing nothing.
//    So on a 1m chart the weekly and monthly datasets are no longer fetched at
//    all. Fewer real datasets per script is exactly what keeps an alert inside
//    the server\'s budget, which is where the calculation errors came from.
//    The requests stay DYNAMIC, as asked. The same mechanism means an alert
//    created with the table switched off fetches no extra timeframe at all,
//    because tblOn is part of every flag.
//    TIDIER AND CHEAPER: each of the eighteen flag lines used to call
//    timeframe.in_seconds on its own string, and now calls it once into a
//    named constant that both tests read. The flags stay plain inline
//    expressions on purpose — wrapping them in a helper function would let
//    Pine promote them from simple to series, and a series flag would make
//    the timeframe argument of request.security series too, which is exactly
//    the thing being avoided here.
//    AND ONE MICRO-OPTIMISATION in the table renderer: tsize(tblRowSz) was
//    being recomputed six times per row on every redraw. It is hoisted to a
//    single value.
//    NO INPUT is added, removed or moved, so saved settings carry over and no
//    settings reset is needed. NOT ONE ENGINE RULE CHANGES: the chart
//    drawings, the three tiers, the alerts and every structure classification
//    are identical to v15.17.
//
//  v15.17  ONE CANDLE CAN NO LONGER SUPPLY A LEVEL AND TRIGGER ITS OWN
//  PROMOTION ON THE SAME BAR.
//    Reported on two charts. FIRST: a single wide candle swept the ceiling AND
//    the floor, and both pivot-2 promotions fired on that one bar, so the whole
//    range collapsed onto that candle — its high became the possible BOS and its
//    low the possible CHOCH, neither of them a pivot anything had confirmed.
//    SECOND: a BOS line anchored back at its original pivot but drawn at the low
//    of a much later candle, because that candle wicked below the floor, closed
//    back above it, and its own high cleared the opposite reference — sweep and
//    trigger on one bar again, so the floor was promoted down to the wick of the
//    very candle that made it.
//    THE CAUSE, and it is the same one both times. The promotion asks two
//    questions: was the level swept, and has price since taken out the opposite
//    reference. Nothing stops BOTH from being answered by the SAME candle. A
//    wide bar wicks through the level and, in the same range, trades past the
//    reference on the other side — so the sweep is recorded and the trigger
//    fires before the bar has even closed, and the level moves to a wick that
//    is one bar old and has been confirmed by nothing.
//    THE FIX, and it is deliberately the narrowest one that works. When the
//    trigger is satisfied ON THE BAR THAT MADE THE SWEPT EXTREME, that sweep is
//    flagged, and while it carries the flag the promotion additionally requires
//    the swept wick to have been confirmed as a pivot by the 3-CANDLE PULLBACK
//    RULE. A wick made on the current bar can never be confirmed on its own bar,
//    so the collapse cannot happen; if the pullback arrives later, the promotion
//    fires then, exactly as before. THE FLAG IS PER SWEEP: recording a new,
//    deeper swept extreme clears it, and a level re-created by any route starts
//    clean.
//    WHAT IS NOT TOUCHED, on purpose. If the trigger arrives on a LATER bar than
//    the sweep — which is the ordinary case, and the GBPCHF sequence v15.14
//    change 2 was written for — nothing is flagged, nothing is checked, and the
//    promotion behaves exactly as it did in v15.16. v15.14 change 2 is intact:
//    an unconfirmed swept wick still takes the level. Only the one-candle case
//    is gated.
//    WHAT THE FIRST CHART NOW DOES: the ceiling is not promoted, so it stays on
//    the original pivot and the break is measured there. The candle then closes
//    through it and the opposite side re-anchors with f_retExtInc, whose window
//    includes the break candle (v15.14 change 1) — so the new floor lands on
//    that candle's low, the deepest point of the leg. The original pivot keeps
//    the BOS, the newest low becomes the CHOCH*, which is the outcome asked for.
//    ALL SIX PROMOTION SITES: main, internal and inner, up-break and down-break.
//    ONE FIELD is added to the SLine type and one bool to each of the four
//    internal and inner sweep records. NO INPUT is added, removed or moved, so
//    saved settings carry over and no settings reset is needed. The 3-candle
//    rule itself, the swing detector, the IDM gate, the alternation, the
//    retracement alerts and every drawing rule are untouched.
//
//  v15.16  INNER ALERTS COMPLETED · TWO NEW LIVE-LINE PAIRS · 50% RANGE LINES ·
//  A MANUAL LIMIT ON HOW MANY MARKS STAY ON THE CHART.
//  Four additions, all requested, and every one of them appended at the end of
//  the script so that no existing input moves. Saved settings carry over and no
//  settings reset is needed. Not one engine rule is touched: the classification
//  of every BOS, CHOCH, iBOS, iCHOCH, nBOS and nCHOCH is identical to v15.15,
//  and the diff to the engine block is four lines, all of them pushing a
//  drawing that was already being created into an array so it can be counted.
//
//  1. THE INNER TIER CAN NOW BE ALERTED ON FROM THE CONDITION DROPDOWN.
//     The inner engine has fired alert() calls for bullish nBOS, bearish nBOS,
//     bullish nCHOCH, bearish nCHOCH and "any inner structure event" since the
//     tier was added, but NOT ONE of them had an alertcondition entry — and
//     alertcondition is what puts an event in the Condition dropdown of
//     TradingView's Create Alert dialog. The main and internal tiers had their
//     entries; the inner tier was reachable only through "Any alert() function
//     call". SEVEN alertcondition entries are added: Bullish nBOS, Bearish
//     nBOS, Bullish nCHOCH, Bearish nCHOCH, Any nBOS, Any nCHOCH and Any inner
//     structure event. Two alert() toggles are added with them — "Any nBOS
//     alert" and "Any nCHOCH alert" — because those two combined streams
//     existed for the internal tier and not for the inner one. Every new entry
//     obeys the arming gate exactly like the ones already there.
//     The five arming slots also gain nBOS, nCHOCH and "Any inner structure
//     event" as triggers, so an inner event can now arm the gate as well as
//     pass through it.
//
//  2. iBOS* / iCHOCH* AND nBOS* / nCHOCH* LINES.
//     The main engine draws its two live levels as the extended BOS* and CHOCH*
//     lines. The internal and inner engines tracked exactly the same pair of
//     levels and drew nothing at all until one of them broke, so the level
//     price was about to test was invisible on two of the three tiers. Both
//     pairs are now drawn, on the main engine's own naming rule: the level
//     whose break would print a BOS gets the *BOS* style, the level whose break
//     would print a CHOCH gets the *CHOCH* style, and a line therefore changes
//     name and style the instant its tier's trend flips. While a tier's trend
//     is still neutral — which every fresh range starts out as — BOTH of its
//     lines read *CHOCH*, because the first break in either direction is a
//     CHOCH. Four new style groups, each with the full set the main groups
//     have: line colour, text colour, style, width, label text, text size,
//     show-text, vertical and horizontal placement. Two on/off switches.
//     They are redrawn in place on the last bar, so the pair costs four
//     drawings no matter how long the chart is, and they can never crowd out
//     the iBOS / iCHOCH history.
//
//  3. THE 50% LINE OF EVERY RANGE.
//     The equilibrium of the BOS*/CHOCH* pair, of the iBOS*/iCHOCH* pair and of
//     the nBOS*/nCHOCH* pair. Each has its own percentage — 50 by default, so
//     it is the midpoint — measured UP FROM THE LOWER of the two lines, which
//     is what makes 50 read the same whichever way the tier's trend is running.
//     Each has the same complete style set the structure groups have, its own
//     on/off switch, and an option to write the percentage into the label so it
//     cannot go stale when the number is changed. One shared setting decides
//     where all three lines start: the older level, the newer level, or the
//     current bar.
//     A LINE NEEDS BOTH OF ITS LEVELS. Every break empties the side it broke
//     and it stays empty until the 3-candle rule confirms the next pivot, so
//     there is no range to halve in between and the line is simply not drawn.
//     The main line is on by default, the internal and inner ones are off,
//     because three equilibrium lines at once is a busy chart.
//
//  4. MAX MARKS TO KEEP, ONE SETTING PER TIER.
//     "Max BOS / CHOCH marks to keep" sits in the Engine group, "Max iBOS /
//     iCHOCH" and "Max nBOS / nCHOCH" in the Internal + Inner group. 0 on all
//     three keeps the behaviour every earlier version had. Any other number
//     keeps that many of the most recent structure lines on that tier and
//     deletes the rest oldest-first, along with their labels and their strong-
//     point stamps. Set 3 and the chart carries the last three breaks only.
//     DISPLAY ONLY, all three: the engine still computes every break on every
//     bar, and every alert still fires whatever these are set to. The live
//     BOS* / CHOCH* pair and the two new live pairs are never counted or
//     deleted by them — those are the current levels, not history.
//
//  v15.15  THE PIVOT-2 PROMOTION NO LONGER MOVES THE ANCHOR.
//    Reported on BTCUSD 15m after v15.14: a main BOS and a main CHOCH printing
//    on adjacent candles a few points apart.
//    THE CAUSE, and it is v15.5 all over again. v15.14 promoted the level to
//    the swept wick AND set the new line x1 to the SWEEP BAR:
//        int sbC = ceilS.swpB
//        ceilS := mkSLine(spC, sbC, sdC, drawOk)
//    When that ceiling is later broken the opposite side re-anchors with
//    f_retExtInc(ceilS.x1, false), which scans from x1 + 1. With x1 sitting on
//    the sweep bar, the SWEEP CANDLE OWN LOW is outside the window — and so is
//    everything between the original pivot and the sweep. refP never covered
//    the sweep bar either, so nothing else compensates. The new floor comes out
//    shallower than the leg really was, and the next candle closes straight
//    back through it: BOS, then CHOCH, one bar apart.
//    v15.5 fixed the identical fault for the equal-price retest and stated it
//    plainly: move x1 forward and that window shrinks, so the floor lands on a
//    shallow low inside the real leg. Invariant 2 says the same thing —
//    re-creating the trend-side line inside a range must not re-scope it.
//    THE FIX: the promotion takes the swept wick PRICE and keeps the ORIGINAL
//    anchor bar, at all six sites. The rule you asked for in v15.14 is intact —
//    the level still becomes the swept wick — only the scope is preserved.
//    MEASURED over 83,239 randomised promotion-then-break sequences: the two
//    give a different floor in 9,035 of them (10.9%), and the kept anchor is
//    deeper or equal in every single one. It never raises a floor.
//    Six lines. No input added, removed or moved; no settings reset needed.
//
//  v15.14  TWO RULE CHANGES, BOTH REQUESTED, BOTH IN ALL THREE ENGINES.
//
//  1. THE BREAK CANDLE NOW COUNTS TOWARDS THE OPPOSITE LEVEL.
//     Reported on GBPCAD 4h: candles 1-4 formed the pullback that confirmed a
//     swing high; candle 5 wicked BELOW candle 4 low, then closed above the
//     ceiling and printed the iBOS. The new floor landed on candle 4 low, not
//     on candle 5 wick, even though candle 5 wick was the deepest point of that
//     pullback and happened before the break close.
//     THE CAUSE, one clause in f_retExtreme:
//         int jEnd = sz - 1          // index of the CURRENT bar
//         for j = j0 to jEnd - 1     // <- the break candle is skipped
//     It implemented the documented rule literally — "the extreme wick STRICTLY
//     BETWEEN the broken pivot candle and the break candle, both excluded".
//     Excluding the pivot candle is right. Excluding the BREAK candle is not:
//     it discards the deepest point of the leg in exactly the case that matters
//     most, where the candle sweeps the previous low, takes the liquidity, and
//     then closes up through the level. That wick is the strong low.
//     THE FIX: f_retExtInc, identical except that it scans to jEnd, is used at
//     the SIX opposite-side re-anchors — main, internal and inner, up-break and
//     down-break. It can only ever deepen the level, and only when the break
//     candle genuinely made the leg extreme; on an ordinary break candle the
//     two functions return the same bar, so nothing else moves.
//     NOT CHANGED, deliberately: the six opposite-REFERENCE calls that arm the
//     pivot-2 promotion, the two IDM taken-past tests, and the two Module 1
//     alternation re-arms all still use the exclusive f_retExtreme.
//     The strong-point stamps follow the new level, because they read the same
//     result — so the marks now sit on the sweep wick.
//
//  2. A PIVOT-2 PROMOTION TAKES THE SWEPT WICK.
//     Reported on GBPCHF 4h: P1 was the ceiling and P2 the floor, confirmed by
//     the P1-P2 pullback satisfying the 3-candle rule. P3 then wicked ABOVE P1
//     and closed back inside — a sweep — and P4 took out P2. P4 became the
//     possible CHOCH, but P3 never became the possible BOS, so the later break
//     was measured against P1 at 1.0922 instead of P3 at 1.0944.
//     THE CAUSE: the promotion called f_confExt, which requires the replacement
//     to be a 3-candle-CONFIRMED pivot (v13.4, extended to internal in v13.6).
//     P3 high was never confirmed, so f_confExt returned na, no promotion fired
//     and the original pivot kept the line — which is what the rulebook said.
//     THE RULE, as requested: once a pullback has satisfied the 3-candle rule,
//     no later pullback has to satisfy it again until a break happens. So the
//     swept extreme itself takes the level.
//     THE FIX: the six promotions now take the recorded sweep extreme
//     (ceilS.swpP / florS.swpP, iCeilSwp / iFlorSwp, nCeilSwp / nFlorSwp) and
//     its bar, instead of asking f_confExt for a confirmed pivot. f_confExt has
//     no callers left; the definition is kept so this is easy to revert.
//     BOTH TRIGGERS ARE UNCHANGED. A promotion still requires a genuine sweep
//     of the level AND price then taking out the opposite reference. This is
//     not "any wick can become a level".
//     IT DOES REVERSE INVARIANT 4 for this one path, which v13.4 added on
//     purpose. Invariant 4 has been reworded rather than quietly broken. If a
//     mark ever lands on a wick you do not consider structure, this is the
//     first thing to look at.
//
//  MEASURED IN NODE against both reported sequences: change 1 moves the floor
//  from candle 4 low to the break candle wick and is inert on an ordinary break
//  candle; change 2 leaves the ceiling on P1 under the old code and promotes it
//  to P3 swept wick under the new one, and is inert when no sweep occurred.
//  NO INPUT is added, removed or moved, so saved settings carry over and no
//  settings reset is needed. Module 1, the 3-candle rule itself, the alternating
//  and free-running streams, the retracement alerts and the arming gate are all
//  untouched. Existing alerts should be recreated only if you want them to see
//  the new levels.
//
//  v15.13  EMA IS OFF BY DEFAULT AND HAS A MASTER SWITCH AT THE TOP.
//    NEW INPUT: "▶ EMA — show exponential moving averages", off by default,
//    drawn in group 0 Master Visibility Toggles alongside MASTER A and B.
//    While it is off the module is completely inert: no line, no ribbon, no
//    label, and no EMA cross alert can fire. Turn it on and the EMAs appear
//    exactly as group 9 has them set — 9, 21, 50 and 200, with 100 available.
//    IT IS A GATE, NOT AN OVERRIDE. Unlike MASTER A and MASTER B, which force
//    their whole group on and ignore the individual boxes, this one can only
//    silence the module. It will never switch on an EMA you unticked. That is
//    deliberate: a show/hide switch for one module wants to preserve your
//    per-EMA choices, not overrule them.
//    The five per-EMA checkboxes keep their previous defaults, because a
//    master that turned on and drew nothing would be worse than useless.
//    WHERE IT SITS: declared AFTER every other input and placed on screen with
//    group = gTog. A group renders where its earliest-declared member sits, so
//    it appears at the top while costing no declaration shift — all existing
//    inputs keep their positions and saved settings carry over untouched.
//    ta.ema still runs on every bar whatever the switch says, because a ta.*
//    function must to keep its state correct. It is one multiply and one add.
//
//  v15.12  LONDON KILLZONE DEFAULT SESSION IS NOW 0600-0900.
//    Was 0700-1000. One default value; the input itself has not moved and no
//    input has been added or removed, so every saved setting still lines up.
//    It drives the London killzone BOX and the London session VOLUME PROFILE
//    together, because both read the same window, and the killzone hour shift
//    still applies on top of it.
//    A DEFAULT ONLY TAKES EFFECT ON A FRESH START. A chart that already has
//    this indicator on it keeps whatever is saved in its settings — it will
//    still say 0700-1000. To pick the new default up, either type 0600-0900
//    into the London session field yourself, or remove the indicator and
//    re-add it (Defaults -> Reset settings does the same).
//
//  v15.11  EMA MODULE ADDED TO THE COMPLETE INDICATOR.
//    The EMA module built for the split structure file is now here too, so one
//    file carries everything: the three structure engines, volume profiles,
//    VWAP, killzones, the MTF table, the alert system and now EMAs.
//      · five EMAs — 9 / 21 / 50 / 100 / 200, each with its own on-off,
//        length, colour and width on one row (100 is off by default)
//      · ribbon fill between EMA 1 and EMA 2, coloured by which is on top
//      · EMA 1 / EMA 2 cross alerts, delivered through the same "Any alert()
//        function call" alert as everything else and obeying the arming gate
//      · last-bar labels with an offset so they sit clear of price
//    All five EMAs are CALCULATED whether or not they are drawn. ta.* functions
//    must run on every bar to keep their state correct, and it keeps the ribbon
//    and the cross right while a line is hidden. An EMA is one multiply and one
//    add per bar, so the cost is not measurable next to the rest of this script.
//    EVERY INPUT IS APPENDED AT THE END (invariant 6). All 425 existing inputs
//    keep their declaration positions, so saved settings carry over and NO
//    settings reset is needed. No engine, rule, level, mark or existing alert
//    changes. The EMA lines are ON by default — untick EMA 1/2/3/5 in group 9
//    if you want the chart exactly as it was.
//
//  v15.10  THE RETRACEMENT LATCH WAS SPENT WHERE NO ALERT COULD FIRE.
//    REPORTED: XAUUSD 1m, retracement alerts at 30% on BOS/CHOCH and
//    iBOS/iCHOCH. An armed alert created 17:04:36 armed at 17:05:16 and then
//    said nothing until 17:41:00 — a bar boundary — while an equivalent alert
//    created at another moment reported touches mid-candle at real seconds
//    (20:00:23, 20:58:54). Same settings, same percentage, different behaviour.
//    THE FAULT: the "already reported" latch was set inside the touch test —
//        if not mainRetFired and not _mNewLvl
//            if (trendDir == 1 and low <= mLvl) or ...
//                rtMainRet    := true
//                mainRetFired := true      <- here
//    which runs at global scope on EVERY bar, including every bar of the
//    historical replay TradingView performs when an alert is created. Alerts
//    can never fire on historical bars, so the first touch of a level was
//    consumed by history and the level arrived at the live edge already spent.
//    It could then only report again once the leg made a new extreme and
//    rebuilt the level — and mRunHi / mRunLo only move at a BAR CLOSE, so the
//    recovery landed on a bar boundary. That is the :00 timestamp, and the
//    35 minutes of silence before it.
//    IT WAS NEVER ABOUT THE ARMING GATE. Modelled tick by tick with Pine
//    semantics — per-tick var rollback, the bar-close-gated extreme, the
//    _mNewLvl guard, alert.freq_once_per_bar and a historical replay phase —
//    the old code behaves IDENTICALLY with the gate on and off: both swallow
//    the live touch at 17:05:30 and report at 17:07:30 instead. Whether an
//    alert looked correct depended only on where in the leg it was created.
//    The gate made it more visible because a gated alert is usually created
//    deliberately, mid-leg, on a level price has already been trading around.
//    THE FIX: the touch test now only REPORTS. The latch is burned after the
//    gate, and only where an alert can actually be delivered:
//        if almOk and barstate.isrealtime
//            if rtMainRet
//                mainRetFired := true
//            ...
//    History can no longer consume a level, and neither can a shut gate.
//    MEASURED: with the fix both configurations fire at 17:05:30, on the touch,
//    at a real second — armed and non-armed now behave identically, which is
//    what was asked for. No alert that fires today is lost.
//    Three lines moved. No input added, removed or moved, so saved settings
//    carry over and NO settings reset is needed. No engine, rule, level, mark
//    or percentage changes. Existing alerts must be DELETED AND RECREATED —
//    they run their own frozen copy of the old code.
//
//  v15.9  ALL FOUR KILLZONES ON THE LINE SWITCH, AND THE LINES ARE NAMED.
//    1. WHY LONDON CLOSE HAD NO LINES. Nothing was wrong with the line code —
//       f_kzEdge was already called on the London Close queues. v15.7 had taken
//       the London Close BOX out of MASTER B, so with that box off there was no
//       box for the lines to attach to. kcOn goes back to
//           kcOn = mstB or kcOnI
//       so MASTER B is once again all four killzone boxes. THIS REVERSES HALF
//       OF v15.7 and is deliberate. The other half stands: the London Close
//       SESSION PROFILE is still NOT driven by MASTER B (cOn = cOnI), so the
//       master still draws boxes and no volume profiles. Checked over all 64
//       switch combinations against the new specification: 0 disagreements.
//    2. THE LINES NOW CARRY THEIR KILLZONE NAME, with one set of controls for
//       all four instead of four separate groups:
//         - Text position   Above the top line (default) / Under the bottom line
//         - Text align      Left / Center (default) / Right
//         - Text colour     one picker for all four
//         - Text size       one dropdown for all four
//       The name itself is still each killzone own Text field, so they stay
//       individually renameable and an empty field means no label for that one.
//       While the line switch is on these four settings REPLACE each killzone
//       Text vert. / Text horiz. / Colour / Size; switch it off and every one of
//       those comes straight back untouched.
//       Mechanically the box text is cleared with box.set_text and the label the
//       box pushed into its parallel label queue is blanked, so a killzone can
//       never print its name twice — the Outside Top / Outside Bottom setting
//       puts the name in a LABEL rather than in the box, and both paths are
//       handled.
//    LIFETIME, the part that carries real risk (see v13.6 bug #4): the label
//    queue is wiped in full alongside the line queue on every last-bar
//    execution and shares the same 120-line cap, so at most 120 lines and 60
//    labels exist at once against TradingView budgets of 500 each. Modelled
//    over 300 days x 5 executions at 1, 8 and 15 days of sessions, with and
//    without text: counts exact every time, zero orphaned lines or labels, and
//    nothing created at all while the switch is off.
//    FOUR INPUTS APPENDED after the three from v15.8. All 421 earlier inputs
//    keep their declaration positions, so saved settings carry over and no
//    settings reset is needed. No engine, rule, mark or alert is touched.
//
//  v15.8  KILLZONE BOXES AS TOP AND BOTTOM LINES ONLY — one switch.
//    NEW, in the toggle list at the top of the settings:
//      - Killzone boxes — top and bottom line only   (off by default)
//      - Line colour  ·  Line width
//    ON  = all four killzone boxes are drawn as two solid horizontal lines,
//          one on the session high and one on the session low. The fill and
//          the left/right edges are suppressed. The session text is untouched.
//    OFF = every box returns to its own Fill / Border / Border style / Border
//          width exactly as set per killzone. It overrides, it never
//          overwrites, so nothing of yours is changed underneath.
//
//    TWO CONSTRAINTS SHAPED THE IMPLEMENTATION, both worth knowing.
//    1. Pine boxes have no per-side border control — border_color, _width and
//       _style apply to all four sides at once. So a top-and-bottom-only box
//       cannot be asked for directly. The box is made fully transparent
//       (bgcolor and border_color set to na) and two real lines are drawn on
//       its top and bottom edges, read back from the box itself with
//       box.get_left / _top / _right / _bottom, so the lines cannot drift from
//       the session window they belong to.
//    2. f_kzbox is defined 138 input declarations BEFORE the end of the file,
//       so it cannot read an appended input. Declaring the three new inputs
//       next to it would have shifted 138 inputs and cost a full settings
//       reset. Instead the drawing is DEFERRED to the end of the script (the
//       v14.0 / v14.1 pattern) where it reads the boxes back out of the
//       kqA / kqL / kqN / kqC and kqAv / kqLv / kqNv / kqCv queues they were
//       already stored in. Drawing objects keep whatever state they were last
//       given, so restyling them on the last bar is all that is needed.
//
//    DRAWING BUDGET, because v13.6 bug #4 was exactly this class of mistake —
//    an unbounded queue that made TradingView start deleting the oldest
//    drawings, main BOS/CHOCH lines included. The queue is wiped in full and
//    rebuilt on each last-bar execution and is capped at 120 lines. Modelled
//    over 400 days x 5 executions per day at 1, 3, 8 and 15 days of sessions:
//    the queue never exceeded the cap, held exactly two lines per visible box
//    every time, and every line created was either still queued or deleted —
//    zero orphans. With the switch off it creates no lines and touches no box.
//    Worst case 120 of the 500-drawing budget; at the default 1 day it is 16.
//
//    THREE INPUTS, ALL APPENDED AT THE VERY END (invariant 6) and placed on
//    screen with group = gTog, which is why they appear at the TOP of the
//    settings: a group renders where its earliest-declared member sits. All
//    418 existing inputs keep their exact declaration positions, so saved
//    settings carry over and NO settings reset is needed. No engine, rule,
//    mark or alert is touched.
//
//  v15.7  MASTER B IS NOW THE THREE KILLZONE BOXES AND NOTHING ELSE.
//    MASTER B was wired to five things: the London Close SESSION PROFILE plus
//    all four killzone boxes —
//        cOn  = mstB or cOnI        <- the London Close volume profile
//        kaOn = mstB or kaOnI
//        klOn = mstB or klOnI
//        knOn = mstB or knOnI
//        kcOn = mstB or kcOnI       <- the London Close box
//    so switching it on drew a full session volume profile and a fourth box
//    that were never wanted from a box toggle.
//    NOW: MASTER B drives the Asia, London and New York killzone BOXES only.
//        cOn  = cOnI                <- own checkbox only
//        kaOn = mstB or kaOnI
//        klOn = mstB or klOnI
//        knOn = mstB or knOnI
//        kcOn = kcOnI               <- own checkbox only
//    The London Close session profile and the London Close box are both still
//    available from their own checkboxes in the toggle list; they are simply no
//    longer forced on by the master. With MASTER B OFF nothing at all changes:
//    verified over all 32 combinations of the five individual switches, the
//    resolved flags are identical to v15.6.1. Over all 64 combinations the new
//    wiring matches the requested behaviour exactly; the old wiring differed in
//    24 of them.
//    NOTE, because it is easy to misread on the chart: a session PROFILE
//    appearing while MASTER B is on is MASTER A's doing. MASTER A is ON by
//    default (v15.3) and covers VWAP, the anchored and previous-day profiles
//    and the Asia / London / New York session profiles. MASTER B has never
//    controlled those and still does not.
//    Two resolution lines and one label/tooltip change. NO input is added,
//    removed or moved, so saved settings carry over and no reset is needed.
//    No engine, rule, mark or alert is touched.
//
//  v15.6.1  COMPILE FIX IN THE v15.6 TOOLTIP TEXT. No behaviour change.
//    Error at 2177:706, Syntax error at input "Touches". Two of the sentences
//    v15.6 added to the arm tooltips quoted an option name using RAW DOUBLE
//    QUOTES inside a Pine string literal, so the literal ended at that quote
//    and the parser met a bare identifier immediately afterwards.
//    Both now use single quotes, which this file has always used safely inside
//    double-quoted strings (see the profile tooltips). No escape sequence is
//    relied on anywhere.
//    Not one line of logic differs from v15.6: the arming gate, the three
//    engines, every alert and all 418 inputs are byte-identical apart from the
//    two tooltip strings and this header.
//    The build now runs a string-literal lexer over the whole file, validated
//    against v15.5 as a known-good control, so an unescaped quote cannot ship
//    again.
//
//  v15.6  THE ARMING GATE OPENED WITHOUT PRICE EVER REACHING THE LEVEL.
//    REPORTED: EURUSD 1m. "Arm alerts only after price crosses a level" ON, arm
//    price level 1.15743, "Arms when price" = Either, slot 1 trigger "Price
//    crosses level", only the iCHOCH box ticked, one alert created on "Any
//    alert() function call". Price never traded above 1.15660 all day, yet the
//    iCHOCH alerts arrived from the start and the ARM panel read ARMED.
//    THE FAULT — one line, the price trigger inside f_armHit:
//        0 => lvl > 0 and (dir == "Touches from below" ? high >= lvl
//                        : dir == "Touches from above" ? low  <= lvl
//                        : (high >= lvl or low <= lvl))
//    "Either" is "high >= lvl or low <= lvl". Price sitting BELOW the level
//    satisfies "low <= lvl" on its own, so the gate opened on the FIRST LIVE
//    BAR having touched nothing. The same trivially-true test broke the other
//    two options whenever price was already on the far side: "Touches from
//    above" armed instantly while price was below it, "Touches from below"
//    armed instantly while price was above it. The test asked "is the level on
//    one side of this bar?" when it had to ask "did price REACH the level?".
//    WHY IT SURVIVED SO LONG: v13.6.1's barstate.isrealtime guard stopped
//    history from pre-arming the latch, so the readout correctly showed WAITING
//    across all of history and then flipped to ARMED on the first live bar —
//    which is indistinguishable from an arm that works.
//    THE FIX: a touch now requires the APPROACH SIDE. The previous bar's close
//    fixes which side of the level price is on, and the level must be reached
//    from that side:
//        from below = close[1] <= lvl and high >= lvl
//        from above = close[1] >= lvl and low  <= lvl
//        Either     = whichever of the two applies to where price actually is
//    Everything else about the gate is untouched: still a WICK test, never a
//    close; still evaluated at global scope on every tick, so it trips intrabar;
//    still live bars only; still one permanent latch; still five OR-ed slots,
//    first to fire opens it for good. A gap straight through the level still
//    arms, exactly as an ordinary TradingView price alert would.
//    MEASURED IN NODE against an independent ground truth — the level lying
//    inside a live bar's traded range, never either implementation's own
//    output. Over 3,000 random series: the old "Either" armed in 2,224 of the
//    2,224 series where price NEVER reached the level (100% false) and armed
//    EARLY in 694 of the 776 where it did. The new test produced 0 false arms
//    and armed on the exact touch bar in all 776. Across 2,331 arm events it
//    never once armed without price trading at, or gapping through, the level.
//    Old "Touches from above" false-armed 1,518 times and "from below" 706;
//    both are now 0. On the reported setup the old gate delivered all eight
//    seeded iCHOCH alerts before any touch, the new one delivered none.
//    NO INPUT IS ADDED, REMOVED OR MOVED. Two tooltips are reworded, which is
//    cosmetic, so saved settings carry over and NO settings reset is needed.
//    Nothing else changes: not the 3-candle rule, not any of the three engines,
//    not the retracement alerts, not which events each checkbox reports.
//    Also corrected in that same tooltip: a double-encoded em dash that has
//    been sitting there since at least v14.7 is now a plain em dash.
//
//  v15.5  AN EQUAL-PRICE PIVOT NO LONGER MOVES THE PIVOT. All three engines.
//    THE FAULT: "EQUAL-LEVEL RETEST — a pivot at exactly the standing price keeps
//    the level and re-anchors the line to the most recent touch." The PRICE was
//    kept, but the ANCHOR BAR jumped forward to the later touch, and that anchor
//    is what the opposite side re-anchors from on a break:
//        f_retExtreme(ceilS.x1, false) = the lowest low between the pivot and the
//        break bar.
//    Move x1 forward and that window shrinks, so the new CHOCH* floor lands on a
//    shallow low inside the real leg instead of the leg's actual extreme. Reported
//    on a 3m BTCUSD chart where two highs touched 64538.23 exactly: the retest
//    moved the anchor from P1 to P2, and the CHOCH* floor came out at P3 (64250)
//    instead of P4 (63830). The range was nested inside the true one.
//    It also silently reset that level's born bar, its armed sweep and its opposite
//    reference, so a sweep in progress was forgotten.
//    THE RULE, as it should be: an equal high is the SAME pivot being retested, not
//    a new one. The first touch owns the level. Only a genuinely higher high (or a
//    lower low) may take it, and only into an empty slot.
//    THE FIX: the equal-price retest is removed from all three engines.
//      MAIN     "adoptC or retestC" -> "adoptC", and the chart-start branch now
//               also refuses a pivot equal to the standing level (_cEq / _fEq), so
//               the rule holds while the trend is still neutral too.
//      INTERNAL "if na(iCeilP) or aPhP == iCeilP" -> "if na(iCeilP)"
//      INNER    "if na(nCeilP) or aPhP == nCeilP" -> "if na(nCeilP)"
//    Nothing else changes: not the 3-candle rule, the sweep test, the pivot-2
//    promotion, the opposite-side re-anchor, the retracement alerts or any input.
//
//  v15.4  "Show inner structure" is ON by default. One input default, nothing
//    else. The inner engine has always RUN regardless of this switch — it only
//    ever gated the drawing — so no rule, level, alert or event changes; the
//    nBOS / nCHOCH marks simply appear now instead of being computed silently.
//    Inner marks are sparse by nature: the inner range is neutral until its own
//    first nCHOCH, so expect long stretches with nothing drawn.
//
//  v15.3  NEW DEFAULTS + A MASTER "WHITE STRUCTURE" SWITCH.
//    NEW INPUT, declared FIRST so its group renders above the Master Visibility
//    Toggles: "White BOS / CHOCH lines and text". ON turns all twelve main
//    BOS / CHOCH / BOS* / CHOCH* line and text colours white; OFF returns every
//    one of them to whatever its own colour picker says. It overrides, it does not
//    overwrite — your per-event colours are untouched underneath.
//    It does NOT touch iBOS / iCHOCH or nBOS / nCHOCH, which have their own colours.
//    *** THIS SHIFTS EVERY INPUT BY ONE DECLARATION PLACE. TradingView matches
//    saved settings by declaration order, so reset the settings once after loading
//    (remove the indicator and re-add it, or Defaults -> Reset settings). You have
//    to do that anyway for the eleven new defaults below to appear, so it is the
//    same single reset, not an extra one.
//    NEW DEFAULTS: MASTER A on · rows per profile 24 · max history to draw 5000 ·
//    iBOS/iCHOCH line and text blue · keep previous iBOS/iCHOCH off · keep previous
//    nBOS/nCHOCH off · strong internal mark 💎 · strong inner mark 🧨 · EVENT
//    column off · VWAP band #1 off (#2 and #3 were already off).
//    NOTE: there is no "keep previous" switch for the MAIN engine — it never had
//    one. Main BOS/CHOCH history is bounded by "Max history to draw" alone, now
//    5000 bars. Only internal and inner have that checkbox, and both are now off.
//    No rule, no engine and no alert behaviour changes.
//
//  v15.2  THE PIVOT SHIFT RULE — the main engine used the wrong reference.
//    THE RULE: P1 sweeps P0's high. P1 becomes the new main pivot ONLY IF price
//    then sweeps or closes below P0's PULLBACK EXTREME LOW (without that being a
//    CHOCH). If P1's pullback never reaches that low, P0 KEEPS the level, a close
//    above P0 is the BOS, and a close below P0's pullback extreme is the CHOCH.
//    THE FAULT: the main engine armed the promotion against f_firstConf — the
//    FIRST CONFIRMED counter-swing after the pivot — not the pullback extreme.
//    The first confirmed low is always at or above the extreme, so the shift could
//    fire on a pullback far shallower than the rule allows, handing the level to
//    P1's sweep wick. The BOS was then measured against a high price had never
//    closed above, printed late, and its line was anchored on the sweep bar.
//    Measured over 320,000 bars: the two references differ on 29% of sweeps, and
//    f_retExtreme was never once higher than f_firstConf.
//    THE FIX: the reference is now f_retExtreme — the extreme wick strictly between
//    the pivot candle and the sweep — which is EXACTLY what the internal and inner
//    engines have always used for the same test. The three engines now agree.
//    Mirrored on the floor: the highest high between the floor pivot and the sweep.
//    HONEST NOTE ON SIDE EFFECTS. Structure is a feedback loop, so removing a
//    premature shift changes what happens afterwards. Over the same 320,000 bars
//    98.0% of main BOS/CHOCH events are completely unchanged; 83 disappeared and
//    62 appeared out of 4,219. Those are the legs that were being measured from a
//    sweep wick. There is no version of this fix that leaves them alone.
//    f_firstConf had no other caller and has been removed.
//
//  v15.1  A SWEEP MUST NOT MOVE THE LEVEL (main engine).
//    While the trend is still NEUTRAL the main adopt was
//        adoptC = trendDir == 0 ? phP >= allHi : na(ceilS)
//    with no reference to the standing ceiling's sweep state. So a bar that wicked
//    ABOVE the ceiling and closed back INSIDE — a sweep, not a break — became the
//    new all-time extreme, and two bars later, when the 3-candle rule confirmed it
//    as a pivot, "phP >= allHi" adopted it outright and overwrote the real level.
//    The sweep was recorded and then bypassed: Module 2 says a wick through with
//    the body closing back inside is a SWEEP, and Module 3 says the sweep only
//    ARMS the pivot-2 promotion. This branch ignored both.
//    Effect on the chart: the ceiling jumped onto the sweep wick, so the BOS was
//    measured against a level price had never closed above, printed late, and its
//    line was anchored on the sweep bar instead of the true pivot.
//    FIX: the chart-start adopt is blocked while the standing level carries an
//    armed sweep. The swept pivot keeps the level; a body close beyond it is the
//    BOS, and a body close beyond the opposite side is the CHOCH — which is what
//    the rules always said. Mirrored on the floor.
//    INTERNAL and INNER were checked and need no change: they adopt only into an
//    EMPTY slot ("na(iCeilP) or aPhP == iCeilP"), so a swept level can never be
//    overwritten there. Adding the same guard would have been dead code.
//    Nothing else moves: not the 3-candle rule, the pivot-2 promotion, the
//    opposite-side re-anchor, the retracement alerts, or any input.
//
//  THE 3-CANDLE PULLBACK RULE (Module 1) — swing HIGH; lows mirror
//    1. CANDIDATE = highest high so far; a new OR EQUAL extreme replaces it.
//    2. Three BEARISH pullback candles: c1 = first bearish candle at/after the
//       candidate (its LOW locks); c2 must CLOSE below c1's low; c3 must CLOSE
//       below c2's low -> CONFIRMED.
//    3. Always body-close beyond the previous locked WICK. Never body-to-body.
//    4. Noise PAUSES, never resets. The three need not be consecutive.
//    5. A new OR EQUAL extreme RESTARTS everything.
//    6. Reported on the confirmation bar, ANCHORED at the candidate's bar.
//
//  TWO PIVOT STREAMS — never mix them up
//    MAIN      hiT / loT   free-running.  Read by BOS / CHOCH only.
//    INTERNAL  aHiT / aLoT alternating.   Read by iBOS / iCHOCH and nBOS / nCHOCH.
//
//  THREE NESTED ENGINES
//    MAIN     BOS / CHOCH    · range anchored on the absolute extreme at start
//    INTERNAL iBOS / iCHOCH  · anchored on the main trend-side line's pivot
//    INNER    nBOS / nCHOCH  · anchored on the internal trend-side level
//  Each: adopt into an empty slot · equal price re-anchors to the later touch ·
//  first break of a range = CHOCH (trend starts neutral) · then with-trend = BOS,
//  against = CHOCH + flip · broken side empties and refills · opposite side
//  re-anchors to the retracement extreme · sweeps arm a pivot-2 promotion.
//
//  INVARIANTS — break these and old bugs come back
//    1. Only a confirmed MAIN event starts a new internal range; only a confirmed
//       INTERNAL event starts a new inner range.
//    2. The range anchor is LATCHED for the life of the range. Re-creating the
//       trend-side line inside a range must NOT re-scope or delete levels.
//    3. Main = free-running pivots. Internal and inner = alternating.
//    4. A structure level is a CONFIRMED PIVOT, with ONE deliberate exception
//       (v15.14): a pivot-2 promotion takes the SWEPT WICK. There the sweep of
//       the level plus price then taking out the opposite reference IS the
//       qualification, so no separate 3-candle confirmation is required.
//    5. Never adopt or seed a level price has already closed beyond.
//    6. NEW INPUTS MUST BE APPENDED AT THE END — TradingView matches saved
//       settings by declaration ORDER. Use group = to place them on screen.
//    7. rearm() reads the chart's price buffers, so it must never be called inside
//       request.security(). armHere() is buffer-free and safe in both.
//    8. No variable-offset history references — use bufHi / bufLo / bufDir.
//    9. Pine has no bitwise operators; the EVENT bitmask is unpacked by
//       subtraction. Pine cannot show, hide or disable an input conditionally.
//   10. ANY COMPARISON INVOLVING na YIELDS na, AND if TREATS na AS FALSE. So
//       "x != na" is FALSE, not true. Guard every such test with na(x) first.
//       This one silently disabled the retracement alert from v14.4 to v14.10.
//
//  RETRACEMENT ALERTS — main / internal / inner, one shared percentage
//    The percentage is measured BACK FROM THE EXTREME of the leg: 20 = a shallow
//    pullback near the high, 50 = midpoint, 79 = deep. The anchor is the leg's
//    running extreme since that tier's last event, so the alert does not wait for
//    the 3-candle pullback to confirm the range; if price has not reached the
//    level by confirmation it stays armed and fires afterwards. Fires on TOUCH,
//    intrabar (alert.freq_once_per_bar), only on a move BACK from the extreme,
//    and never on the bar the level was established.
// ═════════════════════════════════════════════════════════════════════════════
// ═════════════════════════════════════════════════════════════════════════════
//  v15.16 ADDITIONS — ONE APPENDED BLOCK.
//  Every input below is declared AFTER all earlier inputs, so no existing input
//  moves and saved settings carry over untouched (invariant 6). Groups render
//  where their earliest-declared member sits, so the three new "max marks to
//  keep" boxes appear inside the Engine and Internal+Inner panels they belong
//  to, while the seven brand-new groups appear at the bottom of the list.
//
//  1. INNER ALERTS COMPLETED. The inner engine already fired alert() calls for
//     bullish/bearish nBOS, bullish/bearish nCHOCH and "any inner event", but
//     NONE of them had an alertcondition entry, so none of them could be picked
//     from TradingView's Condition dropdown — only the "Any alert() function
//     call" route saw them. All seven now have one, and "Any nBOS" / "Any
//     nCHOCH" gain the two alert() toggles they were missing. The five arming
//     slots also gain nBOS, nCHOCH and "Any inner structure event" triggers.
//
//  2. iBOS* / iCHOCH* AND nBOS* / nCHOCH* ACTIVE EXTENDED LINES. The main
//     engine has always drawn its live levels as BOS*/CHOCH* — the extended
//     dotted lines price has yet to break. The internal and inner engines
//     tracked the identical levels (iCeilP/iFlorP, nCeilP/nFlorP) but drew
//     nothing until they broke. They are now drawn on the same rule the main
//     pair uses: the level whose break would be a BOS is the *BOS* line, the
//     one whose break would be a CHOCH is the *CHOCH* line, so a line swaps
//     name and style the moment its tier's trend flips. Both pairs have the
//     full style set the main groups have, and their own on/off switch.
//     They are ONE line each, redrawn on the last bar, so they cost four
//     drawings in total however long the chart is.
//
//  3. 50% LINE OF EVERY RANGE. The midpoint (equilibrium) of the BOS*/CHOCH*
//     pair, of the iBOS*/iCHOCH* pair and of the nBOS*/nCHOCH* pair, each with
//     its own percentage — 50 by default, so it is the midpoint — and its own
//     complete style set. The percentage is measured UP FROM THE LOWER of the
//     two lines, which makes 50 the midpoint whichever way the tier's trend is
//     running. A tier's line only exists while BOTH of its levels do: after a
//     break one side is empty until the next 3-candle pullback refills it, and
//     there is no range to halve until then, so the line disappears — that gap
//     is the engine being honest, not a fault.
//
//  4. MANUAL CAPS ON HOW MANY MARKS STAY ON THE CHART, per tier. 0 keeps the
//     old behaviour on all three. Any other number keeps that many of the most
//     recent structure lines and deletes the rest, oldest first.
// ═════════════════════════════════════════════════════════════════════════════

================================================================================
v16.0 / v2.0  -  TEN TIMEFRAMES REMOVED FROM THE MULTI-TIMEFRAME TABLE
================================================================================
REMOVED : 2m, 3m, 10m, 30m, 45m, 2h, 3h, 6h, 8h, 12h
REMAINING (8) : 1m, 5m, 15m, 1h, 4h, 1D, 1W, 1M

WHY IT IS WORTH SO MUCH. f_tfPack is the whole structure engine written out
again for one requested timeframe, and Pine INLINES a user function at every
call site. Each timeframe the table requests is therefore another complete
copy of that engine in the compiled script. Removing ten of them removes ten
copies.

    compiled tokens   102,987  ->  about 69,700   (the hard limit is 100,256)
    request.security  18 calls ->  8 calls
    engine instances  18       ->  8, on every bar

The third line is the one that matters beyond the token error: fewer requested
datasets per script is exactly what keeps an alert inside TradingView's
server budget, which is where the "calculation error" alerts were coming from.

WHAT IT COSTS. Deleting an input renumbers every input after it, so the 207
inputs that followed the timeframe checkboxes take their neighbour's saved
value once. That is the table styling, the EMA group, the internal and inner
styling, the alert checkboxes, the arm readout and the 50%-line groups.
Inputs 1 to 330 - every structure colour, the volume profiles and the
killzones - are untouched. Re-set the back half of the panel once and it is
done.

ONE CONSEQUENCE WORTH KNOWING. The row for the chart's own timeframe is the
only one that bypasses request.security and reads the live engine directly.
On a chart set to a timeframe that no longer has a row - 3m, 30m, 2h and the
rest of the removed list - the table simply has no self row. The remaining
rows are unaffected and still correct.

NOTHING ELSE CHANGED. Not one engine rule, not one drawing rule, and no alert.

================================================================================
v16.1 / v2.1  -  ORPHAN TEXT FIXED, AND AN ALERT BUDGET
================================================================================
1. TEXT LEFT ON THE CHART WITH NO LINE UNDER IT.  Reported after switching on
   "keep only the latest 10 marks per tier": ten lines survived, but far more
   than ten words did.

   THE CAUSE was mine. The cap kept N lines and 2N labels, on the reasoning
   that a break can print two pieces of text - the BOS/CHOCH word and the
   strong-point stamp. But a CHOCH never stamps, and with strong marks off
   nothing stamps, so 2N labels covered many more than N breaks. The leftovers
   were the text of breaks whose lines had already been deleted.

   THE FIX is to stop guessing a ratio. Each tier now carries a third array
   recording how many labels each line actually produced: f_addLn pushes a line
   and opens a counter, f_addLb pushes a label and increments the counter of
   the line it belongs to. When f_cutOld drops the oldest line it drops exactly
   that line's labels with it - none, one or two, whichever it made. Applied to
   all three tiers and to the two safety prunes as well, so the pairing holds
   whichever route deletes a mark.

2. "Stop after N alert triggers" in the alert group. 0, the default, is
   unlimited and is exactly how every earlier version behaved. Any other number
   is a budget for the whole indicator: every message counts against it, and
   once spent the indicator goes quiet until the alert is recreated.

   HOW IT WORKS. The twenty-four places that used to call alert() now queue
   their message instead, and one block at the end of the script empties the
   queue, counting as it goes. That keeps the budget in one place rather than
   spread over two dozen call sites, and it is why the setting could be added
   without moving any code.

   ONLY LIVE BARS SPEND IT. Replaying history never consumes the budget, so an
   alert created on a long chart starts with all of it.

   It is one budget for the whole indicator, not one per event: with three
   events ticked and a budget of 3, a single busy candle can spend it all.
   TradingView cannot carry the count across alerts - recreate the alert and
   the budget starts again.

ONE INPUT ADDED, appended at the end. No engine rule changes.
